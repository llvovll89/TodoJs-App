const text = document.querySelector(".text");
// 변수
const timecount = new Date();
const timeday = new day();
console.log(timeday);

function titmeInfo(hihi){
    text.innerText = timecount + hihi;
}

titmeInfo("입니다.");